# OpenAI Ads Conversions

This directory is reserved for OpenAI Ads conversion tracking configuration and implementation assets for Heed.

The repository currently contains static assets rather than an application runtime, so the browser Pixel/CAPI implementation should live in the repository that serves the Heed website or application.
